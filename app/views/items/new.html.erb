<div class="search">
  <div class="row">
    <div class="text-center">
      
      <%#form_tag は、form_for と違って、モデルのインスタンスを指定せず <form> タグを生成するメソッド%>
      <%#対応するモデルがないので、form_forを使う必要がない %>
      <%#form_tagでPOSTになるはずだが、new_item_pathをひかせるために、method: :getを入れている %>
      <%= form_tag(new_item_path, method: :get, class: 'form-inline') do %>
        <div class="form-group">
          
          <%# items_controllerに:keywordとして渡す%>
          <%# モデルのインスタンスを指定せず、<input> タグを生成するメソッド%>
          <%# モデルのインスタンスがあったときは f.text_field のように f があったがない%>
          <%# Controller 側では params[:keyword] で取得可能%>
          <%= text_field_tag :keyword, @keyword, class: 'form-control input-lg', placeholder: "キーワードを入力", size: 40 %>
        
        </div>
        <%= submit_tag '商品を検索', class: 'btn btn-success btn-lg' %>
      <% end %>
    </div>
  </div>
</div>

<%= render 'items', items: @items %>